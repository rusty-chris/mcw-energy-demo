Run `init-claude-project` in the current directory.

This wizard generates `.claude/settings.json`, `CLAUDE.md`, and `DECISIONS.md`
tailored to the project by asking about protected directories, passphrase,
Python version, and layout. Safe to re-run.
